function show(type){
    var id = type + "_post";
    var target = document.getElementById(id);
    target.style.display = "block";
}
function hide(type){
    var id = type + "_post";
    var target = document.getElementById(id);
    target.style.display = "none";
}